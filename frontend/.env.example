# Base URL of the deployed Cloudflare Worker API, e.g. https://chinese-food-truck-api.yourname.workers.dev
# Leave blank during local dev — Vite proxies /api to http://127.0.0.1:8787 (see vite.config.ts)
VITE_API_BASE=
