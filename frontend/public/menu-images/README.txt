Drop real menu photos here, named after each item's image_ref_id, e.g.:

  turn0image0.jpg   -> Triple Rice
  turn0image1.jpg   -> Veg Hakka Noodles
  ...

Any item without a matching file automatically falls back to a colored
placeholder tile (see src/components/MenuImage.tsx), so the app works
before you've photographed the menu.
